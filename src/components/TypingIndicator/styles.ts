import {StyleSheet} from 'react-native';
import {
  horizontalScale,
  verticalScale,
} from '../../utils/styleguide/dimensions';

const styles = StyleSheet.create({
  typing: {height: verticalScale(30), width: horizontalScale(100)},
});

export default styles;
