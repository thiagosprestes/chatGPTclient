import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  header: {
    padding: 14,
    borderBottomWidth: 1,
    borderColor: '#ececec',
  },
});

export default styles;
